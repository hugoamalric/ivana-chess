(function (window) {
  const env = {
{% if nginx.tls.enabled %}
    apiBaseUrl: 'https://{{ ivana_chess_api.hostname }}/{{ ivana_chess_api.context_path }}'
{% else %}
    apiBaseUrl: 'http://{{ ivana_chess_api.hostname }}/{{ ivana_chess_api.context_path }}'
{% endif %}
  }
  window['env'] = window['env'] || env
})(this);
