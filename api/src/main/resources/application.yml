ivana-chess:
  server:
    # Server bind address
    bind-address: ${IVANA_CHESS_BIND_ADDRESS:0.0.0.0}
    # Server port
    port: ${IVANA_CHESS_PORT:8080}
    # Context path
    context-path: ${IVANA_CHESS_CONTEXT_PATH:/}
    # Coma-separated list of allowed origins
    allowed-origins: ${IVANA_CHESS_ALLOWED_ORIGINS}
  db:
    # Host of database
    host: ${IVANA_CHESS_DB_HOST:127.0.0.1}
    # Port of database
    port: ${IVANA_CHESS_DB_PORT:5432}
    # Name of database
    name: ${IVANA_CHESS_DB_NAME:ivanachessapi}
    # Name of schema
    schema: ${IVANA_CHESS_DB_SCHEMA:public}
    # Username used to connect to database
    username: ${IVANA_CHESS_DB_USERNAME:ivanachessapi}
    # Password used to connect to database
    password: ${IVANA_CHESS_DB_PASSWORD:ivanachessapi}
  auth:
    # Secret used to generate JWT
    secret: ${IVANA_CHESS_AUTH_SECRET:changeit}
    # Number of seconds for which the JWT is valid
    validity: ${IVANA_CHESS_AUTH_EXPIRATION:604800}
    # HTTP header which contains JWT
    header: ${IVANA_CHESS_AUTH_HEADER:Authorization}
    # HTTP header value prefix
    header-value-prefix: ${IVANA_CHESS_AUTH_HEADER_VALUE_PREFIX:Bearer }
    # Name of cookie used to send JWT
    cookie: ${IVANA_CHESS_AUTH_COOKIE:_ivana_chess_session}

server:
  address: ${ivana-chess.server.bind-address}
  port: ${ivana-chess.server.port}
  servlet:
    context-path: ${ivana-chess.server.context-path}

spring:
  datasource:
    url: jdbc:postgresql://${ivana-chess.db.host}:${ivana-chess.db.port}/${ivana-chess.db.name}?currentSchema=${ivana-chess.db.schema}
    username: ${ivana-chess.db.username}
    password: ${ivana-chess.db.password}
  jackson:
    default-property-inclusion: non_null
  liquibase:
    change-log: classpath:/db/changelog.yml
  mvc:
    throw-exception-if-no-handler-found: true

logging:
  level:
    dev.gleroy.ivanachess: ${IVANA_CHESS_LOG_LEVEL:info}
